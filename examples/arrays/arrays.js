var nomes = [];
nomes[0] = 'Julia';
nomes[1] = 'Alfredo';
nomes[2] = 'Teste';

console.log(nomes);

// Mas, e se?
nomes[4] = 'Sera?';
console.log(nomes);

// Posso tambem criar um array e já inicia-lo
var langs = ['Ruby', 'Javascript', 'C#', 'Java'];
console.log(langs);
