var a = ['Goku', 'Vegeta', 'Gohan', 'Freeza']
a.lenght; // Retorna o numero de elementos no array;
a.sort();
a.push('Trunks'); // Adiciona um novo elemento no fim do array;
a.pop(); //Remove o item no final do array;
a.shift(); //Remove o item no inicio do array
a.unshift('Cell'); //Adicion o item no inicio do array

a.splice(pos, n);
//Onde pos -> index do array;
//n -> numero de items que sera removido a partir do index
