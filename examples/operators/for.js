for (var i = 0; i < 10; i++) {
  console.log(i);
}

//Onde 'var i = 0', é executado antes do loop(bloco de código) iniciar;
//i < 10, define a condição para a execução do loop;
//e i++, é executado a cad vez que o loop é executado
